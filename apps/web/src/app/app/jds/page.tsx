import { Placeholder } from '../_components/placeholder';

export default function JdsPage() {
  return (
    <Placeholder
      title="Job descriptions"
      phase="Phase 6b"
      note="Paste a JD or paste a URL (we scrape the body). Attached per session."
    />
  );
}
