import { Placeholder } from '../_components/placeholder';

export default function SessionsPage() {
  return (
    <Placeholder
      title="Sessions"
      phase="Phase 6b"
      note="Session history lands once we wire the session_events table. Nothing persisted yet."
    />
  );
}
