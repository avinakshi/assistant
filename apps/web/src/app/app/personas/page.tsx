import { Placeholder } from '../_components/placeholder';

export default function PersonasPage() {
  return (
    <Placeholder
      title="Personas"
      phase="Phase 6b"
      note={`Pro-only: customize the system prompt per scenario ("SRE interviews, focus on incidents").`}
    />
  );
}
