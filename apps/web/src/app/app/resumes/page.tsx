import { Placeholder } from '../_components/placeholder';

export default function ResumesPage() {
  return (
    <Placeholder
      title="Resumes"
      phase="Phase 6b"
      note="Upload PDF/DOCX resumes and pick a default. Parsing via BullMQ worker."
    />
  );
}
