import { Placeholder } from '../_components/placeholder';

export default function SettingsPage() {
  return (
    <Placeholder
      title="Settings"
      phase="Phase 6f"
      note="Account, preferences, notifications, devices, data export. Nothing persisted yet."
    />
  );
}
