{
  "name": "@repo/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Interview Copilot Chrome extension — extracts coding problems from LeetCode / HackerRank and fetches AI answers via the api",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.283",
    "@types/jsdom": "^21.1.7",
    "@types/node": "^20.14.0",
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
